#include "main.h"

/**
 * mul - Multiplies the arguments
 *
 * @a: first argument
 * @b: second argument
 * Return: Returns a value
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
